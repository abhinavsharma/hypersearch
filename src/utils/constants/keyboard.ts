export enum FULLSCREEN_KEY {
  KEY = 'F',
  CODE = 'KeyF',
}

export enum EXPAND_KEY {
  KEY = 'I',
  CODE = 'KeyI',
}

export enum SHRINK_KEY {
  KEY = 'Esc',
  CODE = 'Escape',
}

export enum SWITCH_RIGHT_TAB {
  KEY = 'Right',
  CODE = 'ArrowRight',
}

export enum SWITCH_LEFT_TAB {
  KEY = 'Left',
  CODE = 'ArrowLeft',
}
